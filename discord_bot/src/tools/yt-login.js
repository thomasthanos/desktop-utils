#!/usr/bin/env node
require('./load-env').loadEnv();

const { generateOauthTokens } = require('discord-player-youtubei');

console.log(`
┌─────────────────────────────────────────────────────────────────┐
│  ΧΡΗΣΙΜΟΠΟΙΗΣΕ ΛΟΓΑΡΙΑΣΜΟ ΜΙΑΣ ΧΡΗΣΗΣ                            │
│                                                                 │
│  Ο λογαριασμός θα φαίνεται να συνδέεται από IP datacenter, κάτι  │
│  που η Google μπορεί να θεωρήσει ύποπτο. ΜΗΝ βάλεις τον κύριο    │
│  λογαριασμό σου — φτιάξε καινούργιο μόνο γι' αυτό.               │
└─────────────────────────────────────────────────────────────────┘

Θα εμφανιστεί σύνδεσμος και κωδικός. Άνοιξέ τον σε browser, βάλε τον
κωδικό, και δώσε πρόσβαση.
`);

generateOauthTokens()
  .then(() => {
    console.log('\nΑν δεν εμφανίστηκε token παραπάνω, η σύνδεση δεν ολοκληρώθηκε.');
  })
  .catch((error) => {
    console.error('\nΗ σύνδεση απέτυχε:', error.message);
    console.error('Ξαναδοκίμασε. Αν επιμένει, δες το §9 του DEPLOY.md.');
    process.exitCode = 1;
  });
